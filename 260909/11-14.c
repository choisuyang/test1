#include <stdio.h>
#include <stdio.h>

int main (void) {
    char str1[80], str2[80], str3[80];
    char *resp;

    printf("3개의 과일 이름 입력 : ");
    scanf("%s %s %s", str1, str2, str3);

    if(strlen(str1) > )
}